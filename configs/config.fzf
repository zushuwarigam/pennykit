#!/usr/bin/env bash
set -euo pipefail
printf "### %s\n" "$(readlink -f "$0")"

if command -v fzf >/dev/null; then
    if [[ "$(basename "$SHELL")" == "bash" ]]; then
        eval "$(fzf --bash)"
    elif [[ "$(basename "$SHELL")" == "zsh" ]]; then
        eval "$(fzf --zsh)"
    fi
fi
