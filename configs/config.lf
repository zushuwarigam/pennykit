#!/usr/bin/env bash
set -euo pipefail
printf "### %s\n" "$(readlink -f "$0")"

mkdir -p "${HOME}/.config"

if [[ ! -d "${HOME}/.config/lf" ]]; then
    ln -s "${PENNYKIT_HOME}/configs/lf" "${HOME}/.config/lf"
fi
