**/.DS_Store
# Pin lazy-lock.json for deterministic installs
# Generate with `:Lazy lock` in each starter config

.envrc
.netrc

*.log

/logs/*
!/logs/.gitkeep

/tmp/

# Local theme overrides (written by penny theme)
/configs/wezterm/_local_theme.lua
